% Affine model:  Trying to fit a model of the form y_hat = Phi*X +
% c0*mean(X).  Find c0:

X = zeros(7,1);
DT = zeros(7,1);
for expNum = 1:7
    exp = strcat('exp',num2str(expNum),'.mat');
    [X(expNum),DT(expNum)] = runExperiment(exp);
end
    
close all

%DT = X * c0

%% 
x = (-1:0.25:0.5)'
c = [400; 0];
y = x*c( + rand(7,1)*0.5
% y = x + rand(1,10)*0.5 + rand(1,10)*1;
% y = y';
% x = x';
% A = cat(2,x,ones(10,1))

% Y = X*a +b
% DT = [x 1][a;b]
offset = ones(7,1);
Phi = cat(2,offset, X)
%c = mean(DT./X);
c=Phi\DT


figure()
plot(DX,DT_hat)
hold on
plot(X(:,1),DT,'x')
hold off

fprintf('Best fit constant = %1.5f \n', c(1))

%% 




    
