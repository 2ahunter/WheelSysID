function shifted = shiftGC(gc, spokeNum)
% shifts a gain curve down to next spoke (2 rows) 
    shift = 2*spokeNum -2;
    if shift == 0 %i.e., spoke number = 1
        shifted = gc;
    else
    n = length(gc);
    shifted = cat(1,gc(n-shift+1:n),gc(1:n-shift));
    end
end
